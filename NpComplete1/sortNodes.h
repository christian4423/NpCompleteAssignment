#pragma once
#ifndef SORT_NODED_H					// avoid repeated expansion
#define SORT_NODED_H
#include "stdafx.h"
void sortNodes(int matrix[maxVertices][maxVertices + 10], int vertices, int edges, int sort);
#endif