#pragma once
#ifndef TWO_EDGE_H					// avoid repeated expansion
#define TWO_EDGE_H
#include "stdafx.h"
void twoEdges(int matrix[maxVertices][maxVertices + 10], int vertices, int edges, int required, int notRequired);
#endif